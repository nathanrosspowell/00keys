box: node:4.2.6-slim
build:
  steps:
    - script:
      code: export NODE_ENV='testing'
    - npm-install
    - grunt
    - grunt:
      tasks: 'gh-pages'
deploy:
  steps:
    - lukevivier/gh-pages:
        basedir: build
