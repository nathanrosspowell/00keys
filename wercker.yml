box: nodesource/trusty
build:
  steps:
    - script:
        code: export NODE_ENV='testing'
    - npm-install
    - grunt
deploy:
  steps:
    - lukevivier/gh-pages:
        token: '$GH_TOKEN'
        basedir: build
