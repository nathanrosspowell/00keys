-box: node:4.2.6
build:
  steps:
    - script:
        code: export NODE_ENV='testing'
    - npm-install
    - grunt
deploy:
  steps:
    - lukevivier/gh-pages:
        token: '$GH_TOKEN'
        basedir: build
